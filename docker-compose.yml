version: "3.9"

services:
  pki2fa:
    build: .
    container_name: pki2fa-service
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
